/**
 * report-store.js — Legacy shim (no longer imported)
 *
 * The old car/+server.js that imported loadReport() has been replaced.
 * CAR exports now go through GET /api/export/[jobId]/car (exporter.js + kubo-config.js).
 *
 * This file can be safely deleted from the repository.
 */
export {}